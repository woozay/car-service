import React from "react"
import Header from "../components/header"
import Layout from "../components/layout";

export default () => (
    <Layout>
        <Header text="about"/>
        <div>About page</div>
    </Layout>
);
